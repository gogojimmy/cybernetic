= simple_form_for [:admin, @film], html: { class: 'form-horizontal' } do |f|
  = f.input :url, label: "影片內嵌網址", input_html: { class: 'span6' }
  .form-actions
    = f.submit "送出", class: 'btn btn-primary'
